#include <string>
using namespace std;

#ifndef PROCESS_MESSAGES_H // Header guard to prevent multiple inclusion
#define PROCESS_MESSAGES_H

// Declare functions, classes, or variables you want to use in other files
string processMontCo(const string& inputMsg);
    
string processDelCo(const string& inputMsg);

#endif